import{api}from'./api';import type{ActivityLog,MoveTaskPayload,PaginatedResponse,Task,TaskAttachment,TaskComment,TaskLabel}from'../types';
export const taskService={list:(params?:Record<string,string|number|boolean>)=>api.get<PaginatedResponse<Task>>('/tasks/',{params}).then(r=>r.data),projectTasks:(id:number)=>api.get<Task[]>(`/projects/${id}/tasks/`).then(r=>r.data),get:(id:number)=>api.get<Task>(`/tasks/${id}/`).then(r=>r.data),create:(data:Partial<Task>&{assignee_ids?:number[];label_ids?:number[]})=>api.post<Task>('/tasks/',data).then(r=>r.data),update:(id:number,data:Partial<Task>&{assignee_ids?:number[];label_ids?:number[]})=>api.patch<Task>(`/tasks/${id}/`,data).then(r=>r.data),move:(id:number,data:MoveTaskPayload)=>api.patch<Task>(`/tasks/${id}/move/`,data).then(r=>r.data),remove:(id:number)=>api.delete(`/tasks/${id}/`),labels:()=>api.get<TaskLabel[]>('/task-labels/').then(r=>r.data),createLabel:(data:Pick<TaskLabel,'name'|'color'>)=>api.post<TaskLabel>('/task-labels/',data).then(r=>r.data),comments:(id:number)=>api.get<TaskComment[]>(`/tasks/${id}/comments/`).then(r=>r.data),comment:(id:number,content:string)=>api.post<TaskComment>(`/tasks/${id}/comments/`,{content}).then(r=>r.data),attachments:(id:number)=>api.get<TaskAttachment[]>(`/tasks/${id}/attachments/`).then(r=>r.data),upload:(id:number,file:File)=>{const body=new FormData();body.append('file',file);return api.post<TaskAttachment>(`/tasks/${id}/attachments/`,body).then(r=>r.data)},download:async(a:TaskAttachment)=>{const{data}=await api.get(a.download_url,{responseType:'blob'});const url=URL.createObjectURL(data);const link=document.createElement('a');link.href=url;link.download=a.original_name;link.click();URL.revokeObjectURL(url)},activities:(id:number)=>api.get<ActivityLog[]>(`/tasks/${id}/activities/`).then(r=>r.data)};
