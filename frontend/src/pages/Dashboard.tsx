import{ArrowUpRight,BriefcaseBusiness,CheckSquare,Clock3,Plus,WalletCards}from'lucide-react';import{Link}from'react-router-dom';import{Button,EmptyState,ErrorState,LoadingState}from'../components/ui';import{useAuth}from'../features/auth/AuthContext';import{useDashboard}from'../features/work/hooks';import{formatDate}from'../utils/format';
export function Dashboard(){const{user}=useAuth();const query=useDashboard();if(query.isLoading)return <LoadingState/>;if(!query.data)return <ErrorState message="Não foi possível carregar o dashboard."/>;const data=query.data;const stats=[['Projetos ativos',String(data.active_projects),BriefcaseBusiness],['Tarefas pendentes',String(data.pending_tasks),CheckSquare],['Horas este mês',`${data.hours_this_month}h`,Clock3],['Receita do mês','R$ 0,00',WalletCards]] as const;return <><div className="page-head"><div><h1>Bom dia, {user?.first_name} 👋</h1><p>Acompanhe seus projetos e sua equipe.</p></div><Button onClick={()=>location.assign('/projects')}><Plus size={18}/> Novo projeto</Button></div><div className="stats">{stats.map(([label,value,Icon])=><article key={label}><div><span>{label}</span><strong>{value}</strong></div><Icon/></article>)}</div><div className="dashboard-grid"><section className="panel"><h2>Projetos recentes</h2>{data.recent_projects.length?data.recent_projects.map(p=><Link className="project-row" to={`/projects/${p.id}`} key={p.id}><span><b>{p.name}</b><small>{p.client_detail.name}</small></span><strong>{p.progress}%</strong></Link>):<EmptyState title="Nenhum projeto ainda" description="Seus projetos recentes aparecerão aqui."/>}</section><section className="panel"><h2>Próximos prazos</h2>{data.upcoming_deadlines.length?data.upcoming_deadlines.map(p=><Link className="deadline-row" to={`/projects/${p.id}`} key={p.id}><span><b>{p.name}</b><small>{p.client_detail.name}</small></span><time>{formatDate(p.due_date)}</time></Link>):<EmptyState title="Tudo tranquilo por aqui" description="Você ainda não possui prazos próximos."/>}</section></div><section className="panel section-gap"><h2>Atividade recente</h2>{data.recent_activity.length?data.recent_activity.map(a=><div className="activity-row" key={a.id}><i>•</i><span><b>{a.user?.first_name||'Sistema'}</b> {a.action.replaceAll('_',' ').toLowerCase()}</span><time>{new Date(a.created_at).toLocaleString('pt-BR')}</time></div>):<EmptyState title="Nenhuma atividade ainda" description="As atualizações do seu workspace aparecerão aqui."/>}</section><section className="getting-started"><div><span>PRIMEIROS PASSOS</span><h2>Prepare seu workspace</h2><p>Complete estas etapas para aproveitar melhor o DevFlow.</p></div><div><p>1 &nbsp; Personalize seu perfil <ArrowUpRight size={16}/></p><p>2 &nbsp; Cadastre seus clientes <ArrowUpRight size={16}/></p><p>3 &nbsp; Crie seu primeiro projeto <ArrowUpRight size={16}/></p></div></section></>}
