import{useState}from'react';import{Button,EmptyState,LoadingState,Select}from'../components/ui';import{useReport}from'../features/finance/hooks';import{api}from'../services/api';
export function ReportsPage(){const[group,setGroup]=useState('project');const report=useReport(group);const download=async()=>{const r=await api.get('/reports/hours/export/',{responseType:'blob'});const url=URL.createObjectURL(r.data);const a=document.createElement('a');a.href=url;a.download='horas.csv';a.click();URL.revokeObjectURL(url)};const max=Math.max(1,...(report.data?.rows.map(x=>x.hours)||[]));return <><div className="page-head"><div><h1>Relatórios</h1><p>Analise horas por projeto, cliente ou pessoa.</p></div><Button onClick={download}>Exportar CSV</Button></div><div className="toolbar"><Select value={group} onChange={e=>setGroup(e.target.value)}><option value="project">Por projeto</option><option value="client">Por cliente</option><option value="team">Por pessoa</option></Select></div><section className="panel"><h2>Distribuição de horas</h2>{report.isLoading?<LoadingState/>:report.data?.rows.length?report.data.rows.map(x=><div className="report-row" key={x.name}><span>{x.name}</span><div><i style={{width:`${x.hours/max*100}%`}}/></div><b>{x.hours.toFixed(2)}h</b></div>):<EmptyState title="Sem dados para exibir"/>}</section></>}
